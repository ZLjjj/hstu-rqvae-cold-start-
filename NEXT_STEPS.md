# 下一步实验构思：内容语义与协同语义联合建模

## 1. 研究动机

当前 Route A 先将物品的三级 RQ-VAE 码本向量拼接为内容表示 $z_i$，再让所有物品共享同一个线性变换：

$$
v_i = W_0 z_i + b.
$$

其中 $z_i$ 主要描述物品的内容语义，$v_i$ 是最终进入 HSTU 的物品表示。共享矩阵 $W_0$ 能从暖物品行为中学习“内容空间到推荐空间”的全局映射，并将该映射迁移给冷物品；但同一套变换无法充分表达不同物品的个性化协同关系。

希望进一步实现以下几类关系：

| 内容关系 | 协同关系 | 期望的变换结果 |
|---|---|---|
| 相近 | 相近 | 变换后更接近 |
| 相近 | 不相近 | 变换后被拉远 |
| 不相近 | 相近 | 变换后适度拉近 |
| 不相近 | 不相近 | 继续保持较远 |

直观上，可以将内容向量看作物品“原本表达什么”，将个性化变换看作物品“在用户行为中如何被使用”。最终表示同时保留内容语义和协同语义。

## 2. 为什么不直接为每个物品学习完整矩阵

最直接的形式是：

$$
v_i = W_i z_i.
$$

这个方案可以作为诊断实验，但不适合作为最终冷启动模型，主要有三个原因：

1. **冷物品无法学习自己的矩阵。** 严格冷启动物品没有训练交互，因此 $W_i$ 没有协同监督。
2. **容易退化成 ID embedding。** 每个 $W_i$ 只作用于自己的固定向量 $z_i$，训练实际上只能约束结果 $W_i z_i$，无法唯一确定整张矩阵；其效果近似于直接为每个物品学习一个自由向量。
3. **参数量和过拟合风险较大。** 若输出维度为 $d$、内容维度为 $d_z$，完整方案需要 $N d d_z$ 个参数，难以扩展到大规模物品库。

因此，关键不是让每个物品拥有一张完全独立的矩阵，而是让所有物品共享大部分参数，只学习一个可以迁移到冷物品的个性化低秩变换。

## 3. 推荐方案：共享矩阵与个性化低秩残差

将每个物品的变换写成：

$$
W_i = W_0 + \Delta W_i,
$$

其中 $W_0$ 沿用 Route A 的共享变换，个性化部分采用秩为 $r$ 的残差：

$$
\Delta W_i = A\,\mathrm{Diag}(g_i)B,
$$

$$
v_i = \operatorname{Norm}\left((W_0+A\,\mathrm{Diag}(g_i)B)z_i+b\right).
$$

这里：

- $A\in\mathbb{R}^{d\times r}$ 和 $B\in\mathbb{R}^{r\times d_z}$ 由所有物品共享；
- $g_i\in\mathbb{R}^{r}$ 控制物品使用哪些协同变换方向；
- $r\ll\min(d,d_z)$，用于限制参数量和防止退化为自由 ID embedding。

### 3.1 暖物品与冷物品的个性化系数

暖物品可以从协同行为中学习一个个性化系数 $g_i^{\text{collab}}$。为了让冷物品也能得到该系数，再训练内容生成器：

$$
\hat g_i^{\text{content}} = G(z_i).
$$

最终使用按物品热度控制的混合门控：

$$
g_i = \lambda(n_i)g_i^{\text{collab}}
+(1-\lambda(n_i))\hat g_i^{\text{content}},
$$

$$
\lambda(n_i)=\frac{n_i}{n_i+\tau},
$$

其中 $n_i$ 是物品训练交互次数。于是：

- 暖物品交互较多时，更多使用协同个性化系数；
- 长尾物品同时使用内容预测和少量协同信息；
- 严格冷物品满足 $n_i=0$，完全使用 $G(z_i)$，不会依赖未训练的 ID 参数。

为了让内容生成器学习协同规律，可在暖物品上增加蒸馏损失：

$$
\mathcal L_{\text{distill}}
=\left\|G(z_i)-\operatorname{sg}(g_i^{\text{collab}})\right\|_2^2,
$$

其中 `sg` 表示停止梯度。

## 4. 备选方案：多专家变换

若低秩矩阵实现仍然复杂，可以先验证更简单的多专家方案：

$$
W_i=\sum_{k=1}^{K}\alpha_{ik}W_k,
\qquad
\alpha_i=\operatorname{softmax}(G(z_i)).
$$

模型共享 $K$ 个变换专家，每个物品根据内容选择不同的专家组合。专家通过暖物品推荐损失学习协同语义，冷物品则依靠内容门控选择专家。该方案比完整 $W_i$ 更容易训练，也天然支持冷启动。

## 5. 训练目标

总损失建议写为：

$$
\mathcal L
=\mathcal L_{\text{rec}}
+\beta\mathcal L_{\text{distill}}
+\gamma\mathcal L_{\text{reg}}.
$$

- $\mathcal L_{\text{rec}}$：沿用 Route A 的 sampled-softmax 物品检索损失，训练负样本仍只来自暖物品；
- $\mathcal L_{\text{distill}}$：让内容生成器预测暖物品的协同个性化系数；
- $\mathcal L_{\text{reg}}$：约束 $\Delta W_i$ 的大小，避免个性化变换完全破坏 RQ-VAE 内容空间。

可选地加入基于暖物品共现或 ItemCF 相似度的关系损失，使变换后的几何结构与协同关系更加一致。

## 6. 实验矩阵

所有实验继续使用当前严格冷启动协议：固定 371 个冷物品、训练历史/正样本/负样本零冷物品曝光、全物品评估候选集不变。

| 实验 | 个性化方式 | 冷物品处理 | 目的 |
|---|---|---|---|
| A0 | Route A 共享 $W_0$ | 共享变换 | 当前基线 |
| A1 | 每个暖物品独立完整 $W_i$ | 使用 $W_0$ | 诊断参数化上限与过拟合，不作为最终方案 |
| A2 | $W_0+$ 暖物品 ID 低秩残差 | 冷物品残差为 0 | 检查个性化协同参数对暖物品的作用 |
| A3 | 内容生成的低秩残差 $G(z_i)$ | 由内容直接生成 | 验证可泛化的个性化变换 |
| A4 | 协同教师 + 内容学生 + 热度门控 | $n_i=0$ 时只用内容学生 | 推荐主实验 |
| A5 | $K$ 个变换专家 + 内容门控 | 内容选择专家 | 更稳定、易扩展的备选方案 |

为保证归因清晰，第一阶段保持 HSTU、采样数量、优化器、冷物品划分和评估方式不变，只改变物品变换模块。

## 7. 评估与机制验证

### 7.1 推荐指标

继续报告：

- Overall/Cold/Warm AUC；
- HR@10/50/100/200；
- NDCG@10/50/100/200；
- corrected MRR；
- 参数量、训练显存和全物品检索延迟。

主要比较对象是当前 `rqvae_hstu_route_a_warm_neg`：

- Cold AUC：0.674347；
- Cold HR@100：0.185321；
- Cold NDCG@100：0.047944；
- Cold corrected MRR：0.019836。

### 7.2 表示空间验证

除推荐指标外，还需验证“内容语义 + 协同语义”的物理解释。对暖物品对分别计算：

- 变换前内容相似度：$\cos(z_i,z_j)$；
- 行为共现或 ItemCF 相似度；
- 变换后相似度：$\cos(v_i,v_j)$。

将物品对划分为“内容近/远 × 协同近/远”四组，检查变换后的平均距离变化是否符合第 1 节的预期，并计算变换后相似度与协同相似度的 Spearman 相关系数。

## 8. 成功标准

主实验至少应满足：

1. Cold HR@100 和 Cold NDCG@100 超过当前 Route A；
2. Overall/Warm NDCG@100 不出现明显退化；
3. 冷物品不依赖独立 ID 参数，训练曝光审计仍为 0；
4. 参数量和检索延迟可控；
5. 四类物品对的距离变化与“内容语义 + 协同语义”假设一致。

## 9. 预期结论

该实验要验证的核心问题不是“每个物品是否需要一张完全独立的矩阵”，而是：

> 能否从暖物品协同行为中学习多种内容到推荐空间的局部变换规律，并根据冷物品内容为其选择或生成合适的变换，从而在保留内容泛化能力的同时，更准确地表达协同语义。

如果 A3/A4/A5 的冷启动指标优于共享 $W_0$，同时表示空间验证符合预期，就能说明单一全局变换是 Route A 的限制之一，内容条件化的个性化协同变换是有效的改进方向。
